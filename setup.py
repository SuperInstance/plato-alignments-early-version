from setuptools import setup, find_packages
setup(license="MIT",
    name="plato-alignments", version="0.1.0",
      description="Context artifacts at snap points — capture, store, summon agent alignments",
      packages=find_packages(), python_requires=">=3.8")
